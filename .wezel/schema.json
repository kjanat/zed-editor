{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "Aggregation": {
      "description": "How to aggregate measurement values into a conclusion scalar.",
      "type": "string",
      "enum": [
        "sum",
        "mean",
        "median",
        "max",
        "min"
      ]
    },
    "Command": {
      "description": "Cargo subcommand to invoke.",
      "type": "string",
      "enum": [
        "build",
        "bench",
        "test"
      ]
    },
    "DiffField": {
      "description": "Either a boolean (uses `<step.name>.patch`) or an explicit patch filename.",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "string"
        }
      ]
    },
    "EmbeddedSummaryToml": {
      "description": "Summary definition embedded under a step. The summary's `name` and `step` fields are recovered from the map keys (`step.<step>.summary.<name>`).",
      "type": "object",
      "required": [
        "outcome"
      ],
      "properties": {
        "aggregation": {
          "description": "How to combine multiple matching values. Omit when the filter is expected to select a single value.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/Aggregation"
            },
            {
              "type": "null"
            }
          ]
        },
        "bisect": {
          "description": "Trigger bisection when this summary regresses.",
          "default": true,
          "type": "boolean"
        },
        "filter": {
          "description": "Tag key=value filters applied before aggregation.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "outcome": {
          "description": "Outcome name (as emitted by the forager) to aggregate over.",
          "type": "string"
        },
        "samples": {
          "description": "Number of forager invocations of the step to take. Lint requires all summaries on the same step to agree. Default 1.",
          "default": 1,
          "type": "integer",
          "minimum": 0.0,
          "format": "uint"
        }
      }
    },
    "PackageSpecifier": {
      "description": "Either a list of package names or the literal string \"workspace\".",
      "anyOf": [
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "type": "string",
          "enum": [
            "workspace"
          ]
        }
      ]
    },
    "Step_cargo": {
      "description": "Body of a `[step.<tool>.<step_name>]` table. The tool name is recovered from the outer key, not a field; flatten-collected `rest` is forwarded to the forager plugin via `FORAGER_INPUTS` as JSON.",
      "type": "object",
      "required": [
        "build_target",
        "command"
      ],
      "properties": {
        "all_targets": {
          "description": "Build every target (lib, bins, tests, benches, examples) via `--all-targets`.",
          "default": false,
          "type": "boolean"
        },
        "apply-diff": {
          "description": "Apply a patch before running this step. `true` uses `<step name>.patch`; a string overrides the filename.",
          "anyOf": [
            {
              "$ref": "#/definitions/DiffField"
            },
            {
              "type": "null"
            }
          ]
        },
        "benches": {
          "description": "Benchmarks to build, as `--benches` (all) or `--bench <name>` per name. Omit to build none.",
          "anyOf": [
            {
              "$ref": "#/definitions/TargetSpecifier"
            },
            {
              "type": "null"
            }
          ]
        },
        "bins": {
          "description": "Binaries to build, as `--bins` (all) or `--bin <name>` per name. Omit to build none.",
          "anyOf": [
            {
              "$ref": "#/definitions/TargetSpecifier"
            },
            {
              "type": "null"
            }
          ]
        },
        "build_target": {
          "description": "Build target — either a list of package names or the literal string \"workspace\".",
          "allOf": [
            {
              "$ref": "#/definitions/PackageSpecifier"
            }
          ]
        },
        "command": {
          "description": "Cargo subcommand to invoke (`build`, `bench`, or `test`).",
          "allOf": [
            {
              "$ref": "#/definitions/Command"
            }
          ]
        },
        "description": {
          "type": [
            "null",
            "string"
          ]
        },
        "examples": {
          "description": "Examples to build, as `--examples` (all) or `--example <name>` per name. Omit to build none.",
          "anyOf": [
            {
              "$ref": "#/definitions/TargetSpecifier"
            },
            {
              "type": "null"
            }
          ]
        },
        "lib": {
          "description": "Build the library target via `--lib`.",
          "default": false,
          "type": "boolean"
        },
        "profile": {
          "description": "Cargo profile to use, propagated as `--profile <value>`. Omit to use the default profile.",
          "default": null,
          "type": [
            "null",
            "string"
          ]
        },
        "summary": {
          "description": "Summaries emitted by this step, keyed by summary name.",
          "type": "object",
          "additionalProperties": {
            "allOf": [
              {
                "$ref": "#/definitions/EmbeddedSummaryToml"
              },
              {
                "properties": {
                  "outcome": {
                    "description": "**`time_ms`** — cargo's own reported build duration (the `Finished … in <t>` line), in milliseconds. Excludes process startup and dependency resolution.",
                    "type": "string"
                  }
                }
              }
            ]
          }
        },
        "tests": {
          "description": "Test targets to build, as `--tests` (all) or `--test <name>` per name. Omit to build none.",
          "anyOf": [
            {
              "$ref": "#/definitions/TargetSpecifier"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "TargetSpecifier": {
      "description": "Either a boolean selecting every target of the kind, or a list of target names.",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ]
    }
  },
  "title": "Wezel experiment.toml",
  "description": "Top-level shape of `.wezel/experiments/<name>/experiment.toml`.\n\nSteps live under `[step.<tool>.<step_name>]`. The outer map is keyed by tool name and the inner by step name; bodies are wrapped in `toml::Spanned` so the parser can recover file-line order across reopened tables.",
  "type": "object",
  "required": [
    "step"
  ],
  "properties": {
    "description": {
      "description": "Human-readable description of what the experiment measures.",
      "type": [
        "null",
        "string"
      ]
    },
    "step": {
      "description": "`[step.<tool>.<step_name>]`. Two-level map; step names must be unique across tools (enforced in [`parse_experiment`]).",
      "type": "object",
      "properties": {
        "cargo": {
          "description": "Runs `cargo <command>` and records the build time cargo reports",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/Step_cargo"
          }
        }
      },
      "additionalProperties": false
    }
  }
}
