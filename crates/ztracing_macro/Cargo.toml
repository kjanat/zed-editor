[package]
name              = "ztracing_macro"
version           = "0.1.0"
edition.workspace = true
license           = "GPL-3.0-or-later"
publish.workspace = true

[lib]
proc-macro = true

[dependencies]
