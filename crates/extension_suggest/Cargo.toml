[package]
name              = "extension_suggest"
version           = "0.1.0"
edition.workspace = true
license           = "GPL-3.0-or-later"
publish.workspace = true

[lib]
doctest = false
path    = "src/extension_suggest.rs"

[dependencies]
path.workspace = true

[dev-dependencies]
path = { workspace = true, features = ["test-support"] }

[lints]
workspace = true
