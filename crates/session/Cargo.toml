[package]
name              = "session"
version           = "0.1.0"
edition.workspace = true
license           = "GPL-3.0-or-later"
publish.workspace = true

[lib]
doctest = false
path    = "src/session.rs"

[dependencies]
db.workspace         = true
gpui.workspace       = true
serde_json.workspace = true
util.workspace       = true
uuid.workspace       = true

[features]
test-support = [
  "db/test-support",
]

[lints]
workspace = true
